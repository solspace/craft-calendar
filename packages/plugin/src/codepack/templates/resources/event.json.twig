{% set occurrences = calendar.occurrences({
    rangeStart: craft.app.request.post("rangeStart"),
    rangeEnd: craft.app.request.post("rangeEnd"),
    limit: 500,
}) %}

{{ occurrences.toFullcalendar|json_encode|raw }}
