{
  "name": "@cal/scripts",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run clean && npm run watch",
    "build": "npm run clean && npm run compile",
    "watch": "node ./scripts/build.mjs --watch --mode development",
    "clean": "rm -rf ../plugin/src/Resources/js/scripts",
    "compile": "node ./scripts/build.mjs --mode production",
    "lint": "biome check",
    "lint:fix": "biome check --write",
    "test": "jest --maxWorkers=4"
  },
  "author": "Solspace Inc.",
  "license": "MIT",
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
