import { colors } from "@cal/styles/variables";
import styled from "styled-components";

export const EventWidgetWrapper = styled.div`
  //
`;

export const SuccessMessage = styled.p`
  color: ${colors.success};
  margin: 0;
`;
